import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles["container"]}>
      <div>
        This is footer
      </div>
    </footer>
  );
};

export default Footer;